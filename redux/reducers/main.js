import * as actions from '../actionTypes';

const initialState = {
    cart: {
        init: false
    }
}

switch (actions.INIT_CART) {
    case value:
        
        break;

    default:
        break;
}